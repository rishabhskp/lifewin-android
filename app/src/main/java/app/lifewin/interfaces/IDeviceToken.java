package app.lifewin.interfaces;


public interface IDeviceToken {
    /**
     * Method is used for notify the device token
     * @param deviceToken
     */
    void onDeviceToken(String deviceToken);
}
