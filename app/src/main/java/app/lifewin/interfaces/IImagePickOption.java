package app.lifewin.interfaces;


public interface IImagePickOption {
    /**
     * Method is used for taking the selection type<br/>
     * if option=1 gallery and option=2 Camera
     * @param option
     */
    void onImagePickOption(int option);
}
